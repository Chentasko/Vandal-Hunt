using System.Collections;
using System.Collections.Generic;
using UnityEngine;


//This script keeðs in track of how many customers there are currently alive.
//It will condition a game win ONLY when all of these conditions are met: No innocent killed, All cirminals killed.
//Any of the two happening is a game over
public class customerHandler : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
